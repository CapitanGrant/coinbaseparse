# Coinbase Parser

Микросервис для сбора и анализа данных о криптовалютах с Coinbase API.

## Описание

Сервис предоставляет REST API для получения актуальных курсов криптовалют, исторических данных и анализа динамики цен.
Данные автоматически обновляются каждую минуту.

## 🛠 Технологии

- **FastAPI** - современный веб-фреймворк
- **SQLAlchemy 2.0** - ORM для работы с БД
- **MySQL** - база данных
- **Alembic** - миграции БД
- **APScheduler** - планировщик задач
- **Pydantic** - валидация данных
- **Pytest** - тестирование
- **Docker** - контейнеризация

### Предварительные требования

- Docker и Docker Compose
- Python 3.12+ (для локальной разработки)

### Быстрый запуск с Docker

1. Клонируйте репозиторий:
```bash
git clone https://github.com/CapitanGrant/coinbaseparse
cd coinbaseparse
```

2. Создайте файл окружения:
```bash
cp .env

# Основная база данных
MYSQL_ROOT_PASSWORD=root_password
DB_USER=mysql_user
DB_PASSWORD=mysql_password
DB_NAME=crypto
DB_PORT=3306
DB_HOST=mysql


# Тестовая база данных  
TEST_DB_HOST=localhost
TEST_DB_PORT=3305
TEST_DB_NAME=mysql_db
TEST_DB_USER=test_user
TEST_DB_PASSWORD=test
TEST_DB_URL="mysql+aiomysql://user:password@host:port/db"
```

3. Запустите сервис:

```bash
docker-compose up --build

```

4. Для просмотра документации:
```bash
http://http://localhost:8000/docs#
```
